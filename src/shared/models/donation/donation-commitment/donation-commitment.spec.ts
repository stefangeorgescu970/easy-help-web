import { DonationCommitment } from './donation-commitment';

describe('DonationCommitment', () => {
  it('should create an instance', () => {
    expect(new DonationCommitment()).toBeTruthy();
  });
});
